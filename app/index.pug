extends ./templates/_skeleton.pug

block page
	.content(ng-view='' autoscroll)

block header
	nav.navbar.navbar-default.navbar-fixed-top
		.container
			.navbar-header
				button.navbar-toggle.collapsed(type="button" data-toggle="collapse" data-target="#menu" aria-expanded="false")
					span.icon-bar
					span.icon-bar
					span.icon-bar
				a.navbar-brand(href="/")
					img(src="images/logo.png")
					//| <?php echo $data->global->header->logo; ?>
					//br
					//span.slogan
						| <?php echo $data->global->header->slogan; ?>
			#menu.collapse.navbar-collapse.navbar-right
				ul.nav.navbar-nav.
					<?php foreach( $data->global->header->menu as $key => $menu ){
						echo "<li name='{$menu->name}'" . ( !$key ? " class='active'" : "" ) . ">";
						echo "<a href='#/{$menu->url}'>";
						echo "<span class='{$menu->icon}'></span>";
						echo $menu->title;
						echo "</a>";
						echo "</li>";
					} ?>

block footer
	#back-top
		span.glyphicon.glyphicon-menu-up
	footer
		.logo
			img(src="images/logo_white.png")
		.midia.
			<?php foreach( $data->global->midias as $key => $midia )
				echo "<a target='{$midia->target}' href='{$midia->url}'><i class='{$midia->icon}'></i></a>";
			?>
		.name.
			<?php echo $data->global->footer->name . date(" - Y"); ?>
		.credits
			a(target="_blank" href="http:///www.futuraagencia.com.br/")
				span.fa.fa-cube.
				<?php echo $data->global->footer->credits; ?>